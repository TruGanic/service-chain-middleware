export interface ConfirmPickup {
  batchId: string;
  produceType: string;
  supplierId: string;
  farmerName: string;
  pickupLocation: string;
  weightKg: string;
  notes?: string;
  pickupTimestamp: string;
}

